# MES Trading Intelligence System — Python dependencies
# Install into venv: pip install -r requirements.txt
# Note: XGBoost on macOS needs `brew install libomp` first

# UI
PySide6>=6.5.0

# AI / LLM
anthropic>=0.25.0

# Market data
yfinance>=0.2.0
finnhub-python>=2.4.0

# Numerical / scientific
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# Machine learning
scikit-learn>=1.3.0
xgboost>=2.0.0
hmmlearn>=0.3.0

# HTTP / networking
requests>=2.31.0
httpx>=0.25.0
websocket-client>=1.6.0

# Web scraping (for news feeds)
beautifulsoup4>=4.12.0

# Environment & config
python-dotenv>=1.0.0

# Terminal output (legacy SPY tools)
rich>=13.0.0

# Flask trade journal (legacy)
flask>=3.0.0
flask-sqlalchemy>=3.1.0

# Browser automation (optional, for GEX scraping)
playwright>=1.40.0

# Rithmic feed (install separately if available)
# async-rithmic>=0.1.0

# Alpaca feed (optional)
# alpaca-py>=0.13.0
